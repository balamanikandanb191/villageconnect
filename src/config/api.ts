const baseUrl = 'https://0xa8exc3ug.execute-api.us-east-1.amazonaws.com/';

export const API_ENDPOINTS = {
  services: `${baseUrl}/services`,
  contact: `${baseUrl}/contact`,
  requestService: `${baseUrl}/request-service`,
};